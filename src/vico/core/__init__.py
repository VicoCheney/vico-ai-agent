"""
Core subsystem — agent loop, context management, system prompt, and shared types.

Public contracts used by all other subsystems live in ``vico.core.types``.
"""

from __future__ import annotations
